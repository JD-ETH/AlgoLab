//============================================================================
// Name        : Game.cpp
// Author      : 
// Version     :
// Copyright   : Your copyright notice
// Description : Hello World in C++, Ansi-style
//============================================================================
#include <vector>
#include <algorithm>
#include <iostream>
using namespace std;

static vector<vector <int> > trans;
static int n;
bool solve(int r, int b, int mv){
	if (r == n and b == n) {return (mv%2);}
	else if (r ==n) {return false;}
	else if (b == n) {return true;}
	else
}

void do_case(){
	int m;
	cin >> n >> m;
	int r_start,b_start;
	cin >> r_start,b_start;
	trans.clear();
	trans.resize(n+1);
	while (m--){
		int u,v;
		cin >> u >> v;
		trans[u].push_back(v);
	}

	cout << solve(r_start,b_start,0) << "\n";
}

int main() {
	int t;
	cin >> t;
	while(t--){
		do_case();
	}
	return 0;
}
